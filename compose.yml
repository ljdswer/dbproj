services:
  app:
    image: dbproj-app
    networks:
      - dbproj-network
    ports:
      - "127.0.0.1:5000:5000"
    build: ./app/
    restart: no

  mariadb:
    image: mariadb:11.5.2
    container_name: mariadb
    environment:
      MARIADB_ROOT_PASSWORD: root
    volumes:
      - ./storage/db:/var/lib/mysql:z
    ports:
      - "127.0.0.1:3306:3306"
    networks:
      - dbproj-network
    restart: no

networks:
  dbproj-network:
    driver: bridge
